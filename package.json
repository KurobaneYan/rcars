{
  "name": "rcars",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.5.4",
    "react-dom": "^15.5.4",
    "redux": "^3.6.0",
    "standard": "^10.0.2"
  },
  "devDependencies": {
    "husky": "^0.13.4",
    "react-scripts": "1.0.7"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "lint": "./node_modules/.bin/standard",
    "lint-fix": "./node_modules/.bin/standard --fix",
    "precommit": "yarn lint-fix && yarn test -- --coverage",
    "prepush": "yarn lint-fix && yarn test -- --coverage"
  },
  "standard": {
    "env": {
      "browser": true,
      "jest": true
    }
  }
}
